package {{ base_package }}.controller;

import {{ base_package }}.dto.request.{{ entity_name }}RequestDTO;
import {{ base_package }}.dto.response.{{ entity_name }}ResponseDTO;
import {{ base_package }}.service.{{ entity_name }}Service;
{% if pom_features.has_lombok %}
import lombok.RequiredArgsConstructor;
{% endif %}
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("{{ api_prefix }}/{{ entity_name | lower }}s")
{% if pom_features.has_lombok %}
@RequiredArgsConstructor
{% endif %}
public class {{ entity_name }}Controller {

    private final {{ entity_name }}Service {{ entity_lower }}Service;

{% if not pom_features.has_lombok %}
    public {{ entity_name }}Controller({{ entity_name }}Service {{ entity_lower }}Service) {
        this.{{ entity_lower }}Service = {{ entity_lower }}Service;
    }
{% endif %}

    // ── GET /{{ api_prefix }}/{{ entity_name | lower }}s ─────────────────────────────────────────

    @GetMapping
    public ResponseEntity<List<{{ entity_name }}ResponseDTO>> findAll() {
        return ResponseEntity.ok({{ entity_lower }}Service.findAll());
    }

    // ── GET /{{ api_prefix }}/{{ entity_name | lower }}s/{id} ──────────────────────────────────

    @GetMapping("/{id}")
    public ResponseEntity<{{ entity_name }}ResponseDTO> findById(@PathVariable Long id) {
        return ResponseEntity.ok({{ entity_lower }}Service.findById(id));
    }

    // ── POST /{{ api_prefix }}/{{ entity_name | lower }}s ────────────────────────────────────────

    @PostMapping
    public ResponseEntity<{{ entity_name }}ResponseDTO> create(@RequestBody {{ entity_name }}RequestDTO dto) {
        return ResponseEntity
                .status(HttpStatus.CREATED)
                .body({{ entity_lower }}Service.create(dto));
    }

    // ── PUT /{{ api_prefix }}/{{ entity_name | lower }}s/{id} ──────────────────────────────────

    @PutMapping("/{id}")
    public ResponseEntity<{{ entity_name }}ResponseDTO> update(
            @PathVariable Long id,
            @RequestBody {{ entity_name }}RequestDTO dto
    ) {
        return ResponseEntity.ok({{ entity_lower }}Service.update(id, dto));
    }

    // ── DELETE /{{ api_prefix }}/{{ entity_name | lower }}s/{id} ───────────────────────────────

    @DeleteMapping("/{id}")
    public ResponseEntity<Void> delete(@PathVariable Long id) {
        {{ entity_lower }}Service.delete(id);
        return ResponseEntity.noContent().build();
    }

}
